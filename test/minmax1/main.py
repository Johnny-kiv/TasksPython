# Это задача на сортировку
# Будем сортировать ВЫБОРОМ
# This is a sorting task
# I will sort by SELECT
inp = open("input.txt")
im = inp.readline().split(",")
om = []
index = 0
for i in im:
    minx = min(mas)